export type IconType = {
  iconStack: string;
  iconInitial: string;
  iconName: string;
};

export type HoverCardWithLinkType = {
  contentLink?: string;
  trigger: React.ReactNode;
  hoverContent: string;
};
